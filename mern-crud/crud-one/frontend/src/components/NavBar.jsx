import React from "react";
import { AppBar, Toolbar } from "@mui/material";

const NavBar = () => {
  return (
    <>
      <AppBar>
        <Toolbar>
          <p>Material UI initial setup</p>
        </Toolbar>
      </AppBar>
    </>
  );
};

export default NavBar;
